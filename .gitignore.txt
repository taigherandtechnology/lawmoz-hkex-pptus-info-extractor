# 操作系统生成的文件
.DS_Store
Thumbs.db

# 备份文件
*.bak

# 如果未来会使用到 npm/yarn 等前端包管理工具
node_modules/

# IDE/编辑器生成的文件 (如果不需要共享这些配置)
.vscode/    # VS Code
.idea/      # WebStorm/IntelliJ IDEA
*.sublime-project
*.swp       # Vim 临时文件

# 编译/打包生成的文件
*.zip       # 最终打包的扩展程序文件
*.crx       # Chrome 扩展程序打包后的文件（如果直接生成）
dist/       # 如果未来会使用构建工具生成部署文件

# 临时或日志文件
*.log